# 🎨 PharmaPilot System Architecture

```
┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                    PHARMAPILOT SYSTEM                      ┃
┃                  Complete Architecture                     ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛


┌─────────────────────────────────────────────────────────────┐
│                  1. USER INTERFACE LAYER                    │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │           React Frontend (Vite)                      │  │
│  │           Port: 5173                                 │  │
│  │                                                      │  │
│  │  Components:                                         │  │
│  │  - Login/Register UI (Login.jsx)                    │  │
│  │  - Chat Interface (ChatBox.jsx)                     │  │
│  │  - Sidebar Navigation (Sidebar.jsx)                 │  │
│  │  - Message Display (Message.jsx)                    │  │
│  │                                                      │  │
│  │  Context:                                            │  │
│  │  - AppContext (user state, auth)                    │  │
│  │                                                      │  │
│  │  Utils:                                              │  │
│  │  - authApi.js (API calls)                           │  │
│  │  - responseGenerator.js (AI responses)              │  │
│  └─────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ HTTP/REST API
                         │ JSON Requests
                         │ JWT Authentication
                         │
┌────────────────────────▼────────────────────────────────────┐
│                  2. APPLICATION LAYER                       │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │         FastAPI Backend (Uvicorn)                    │  │
│  │         Port: 8000                                   │  │
│  │                                                      │  │
│  │  ┌────────────────────────────────────────────┐    │  │
│  │  │  main.py (FastAPI App)                     │    │  │
│  │  │  - CORS middleware                         │    │  │
│  │  │  - Route registration                      │    │  │
│  │  │  - Health check endpoint                   │    │  │
│  │  └────────────────────────────────────────────┘    │  │
│  │                                                      │  │
│  │  ┌────────────────────────────────────────────┐    │  │
│  │  │  auth.py (JWT Authentication)              │    │  │
│  │  │  - Password hashing (bcrypt)               │    │  │
│  │  │  - Token generation (JWT)                  │    │  │
│  │  │  - Token verification                      │    │  │
│  │  │  - User authentication                     │    │  │
│  │  └────────────────────────────────────────────┘    │  │
│  │                                                      │  │
│  │  ┌────────────────────────────────────────────┐    │  │
│  │  │  routes/auth.py (Auth Endpoints)           │    │  │
│  │  │  - POST /register                          │    │  │
│  │  │  - POST /login                             │    │  │
│  │  │  - GET  /me                                │    │  │
│  │  │  - POST /refresh                           │    │  │
│  │  │  - POST /forgot-password                   │    │  │
│  │  └────────────────────────────────────────────┘    │  │
│  │                                                      │  │
│  │  ┌────────────────────────────────────────────┐    │  │
│  │  │  routes/projects.py (Project CRUD)         │    │  │
│  │  │  - GET    /projects                        │    │  │
│  │  │  - POST   /projects                        │    │  │
│  │  │  - GET    /projects/{id}                   │    │  │
│  │  │  - PUT    /projects/{id}                   │    │  │
│  │  │  - DELETE /projects/{id}                   │    │  │
│  │  └────────────────────────────────────────────┘    │  │
│  │                                                      │  │
│  │  ┌────────────────────────────────────────────┐    │  │
│  │  │  routes/agents.py (AI Agent Interface)     │    │  │
│  │  │  - POST /agents/execute                    │    │  │
│  │  │  - GET  /agents/logs                       │    │  │
│  │  │  - GET  /agents/logs/{id}                  │    │  │
│  │  └────────────────────────────────────────────┘    │  │
│  └─────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ SQLAlchemy ORM
                         │ Database Queries
                         │
┌────────────────────────▼────────────────────────────────────┐
│                  3. DATA LAYER                              │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │         PostgreSQL Database                          │  │
│  │         Port: 5432 (Docker Container)                │  │
│  │                                                      │  │
│  │  ┌────────────────────────────────────────────┐    │  │
│  │  │  Table: users                              │    │  │
│  │  │  ├── id (PRIMARY KEY)                      │    │  │
│  │  │  ├── email (UNIQUE)                        │    │  │
│  │  │  ├── full_name                             │    │  │
│  │  │  ├── password (hashed)                     │    │  │
│  │  │  ├── role                                  │    │  │
│  │  │  ├── is_active                             │    │  │
│  │  │  ├── created_at                            │    │  │
│  │  │  └── updated_at                            │    │  │
│  │  └────────────────────────────────────────────┘    │  │
│  │                                                      │  │
│  │  ┌────────────────────────────────────────────┐    │  │
│  │  │  Table: projects                           │    │  │
│  │  │  ├── id (PRIMARY KEY)                      │    │  │
│  │  │  ├── user_id (FOREIGN KEY → users.id)      │    │  │
│  │  │  ├── name                                  │    │  │
│  │  │  ├── molecule_name                         │    │  │
│  │  │  ├── description                           │    │  │
│  │  │  ├── status                                │    │  │
│  │  │  ├── created_at                            │    │  │
│  │  │  └── updated_at                            │    │  │
│  │  └────────────────────────────────────────────┘    │  │
│  │                                                      │  │
│  │  ┌────────────────────────────────────────────┐    │  │
│  │  │  Table: agent_logs                         │    │  │
│  │  │  ├── id (PRIMARY KEY)                      │    │  │
│  │  │  ├── project_id (FOREIGN KEY → projects.id)│   │  │
│  │  │  ├── agent_type                            │    │  │
│  │  │  ├── input_text                            │    │  │
│  │  │  ├── output_text                           │    │  │
│  │  │  ├── status                                │    │  │
│  │  │  └── created_at                            │    │  │
│  │  └────────────────────────────────────────────┘    │  │
│  └─────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘


┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                    REQUEST FLOW EXAMPLE                     ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

1. USER LOGIN FLOW
   ┌───────────┐
   │  Browser  │
   └─────┬─────┘
         │ 1. User enters email/password
         │
   ┌─────▼──────────┐
   │   Login.jsx    │
   └─────┬──────────┘
         │ 2. authApi.loginUser(email, password)
         │
   ┌─────▼──────────────────────────────────┐
   │   POST /api/v1/auth/login               │
   │   Body: {email, password}               │
   └─────┬───────────────────────────────────┘
         │ 3. FastAPI receives request
         │
   ┌─────▼──────────────────┐
   │  routes/auth.py         │
   │  - Validates credentials│
   └─────┬───────────────────┘
         │ 4. Query database
         │
   ┌─────▼──────────────────┐
   │  PostgreSQL users table │
   │  - Find user by email   │
   │  - Verify password hash │
   └─────┬───────────────────┘
         │ 5. User found
         │
   ┌─────▼──────────────────┐
   │  auth.py                │
   │  - Generate JWT tokens  │
   └─────┬───────────────────┘
         │ 6. Return tokens
         │
   ┌─────▼──────────────────────────────┐
   │  Response: {                        │
   │    access_token: "eyJ...",          │
   │    refresh_token: "eyJ...",         │
   │    token_type: "bearer"             │
   │  }                                  │
   └─────┬───────────────────────────────┘
         │ 7. Store tokens in localStorage
         │
   ┌─────▼──────────┐
   │  AppContext     │
   │  - Set user     │
   │  - Navigate to  │
   │    /chat        │
   └─────────────────┘


2. CREATE PROJECT FLOW
   ┌───────────┐
   │  Browser  │
   └─────┬─────┘
         │ 1. User creates project
         │
   ┌─────▼──────────────────────────────────┐
   │   POST /api/v1/projects                 │
   │   Headers: {                            │
   │     Authorization: "Bearer eyJ..."      │
   │   }                                     │
   │   Body: {                               │
   │     name: "Aspirin Research",           │
   │     molecule_name: "Aspirin"            │
   │   }                                     │
   └─────┬───────────────────────────────────┘
         │ 2. FastAPI verifies JWT token
         │
   ┌─────▼──────────────────┐
   │  auth.py                │
   │  - Decode JWT           │
   │  - Get user_id from     │
   │    token                │
   └─────┬───────────────────┘
         │ 3. Create project
         │
   ┌─────▼──────────────────┐
   │  routes/projects.py     │
   │  - Create Project model │
   │  - Link to user_id      │
   └─────┬───────────────────┘
         │ 4. Save to database
         │
   ┌─────▼──────────────────┐
   │  PostgreSQL projects    │
   │  table                  │
   │  - INSERT new row       │
   └─────┬───────────────────┘
         │ 5. Return project
         │
   ┌─────▼──────────────────────────────┐
   │  Response: {                        │
   │    id: 1,                           │
   │    user_id: 1,                      │
   │    name: "Aspirin Research",        │
   │    molecule_name: "Aspirin",        │
   │    status: "active",                │
   │    created_at: "2025-12-09..."      │
   │  }                                  │
   └─────────────────────────────────────┘


3. AI AGENT EXECUTION FLOW
   ┌───────────┐
   │  AI Agent │
   └─────┬─────┘
         │ 1. Agent sends query
         │
   ┌─────▼──────────────────────────────────┐
   │   POST /api/v1/agents/execute           │
   │   Headers: {                            │
   │     Authorization: "Bearer eyJ..."      │
   │   }                                     │
   │   Body: {                               │
   │     project_id: 1,                      │
   │     agent_type: "patent_search",        │
   │     input_text: "aspirin patents"       │
   │   }                                     │
   └─────┬───────────────────────────────────┘
         │ 2. Verify token & project ownership
         │
   ┌─────▼──────────────────┐
   │  routes/agents.py       │
   │  - Verify project       │
   │    belongs to user      │
   │  - Create agent log     │
   └─────┬───────────────────┘
         │ 3. Save execution log
         │
   ┌─────▼──────────────────┐
   │  PostgreSQL agent_logs  │
   │  table                  │
   │  - INSERT new log       │
   └─────┬───────────────────┘
         │ 4. Return result
         │
   ┌─────▼──────────────────────────────┐
   │  Response: {                        │
   │    id: 1,                           │
   │    project_id: 1,                   │
   │    agent_type: "patent_search",     │
   │    input_text: "aspirin patents",   │
   │    output_text: "Result here...",   │
   │    status: "completed",             │
   │    created_at: "2025-12-09..."      │
   │  }                                  │
   └─────────────────────────────────────┘


┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                    SECURITY LAYERS                          ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

1. AUTHENTICATION (JWT)
   ┌─────────────────────────────────────────┐
   │  Every API request (except login/       │
   │  register) requires:                    │
   │                                         │
   │  Authorization: Bearer <access_token>   │
   │                                         │
   │  Token contains:                        │
   │  - user_id                              │
   │  - expiration time (30 minutes)         │
   │  - signature (verified with SECRET_KEY) │
   └─────────────────────────────────────────┘

2. PASSWORD SECURITY
   ┌─────────────────────────────────────────┐
   │  Passwords are:                         │
   │  - Hashed with bcrypt                   │
   │  - Never stored in plain text           │
   │  - Minimum 8 characters required        │
   │  - Cannot be retrieved (only reset)     │
   └─────────────────────────────────────────┘

3. CORS PROTECTION
   ┌─────────────────────────────────────────┐
   │  Only these origins can access API:     │
   │  - http://localhost:3000                │
   │  - http://localhost:5173                │
   │                                         │
   │  Other domains are blocked              │
   └─────────────────────────────────────────┘

4. SQL INJECTION PROTECTION
   ┌─────────────────────────────────────────┐
   │  All database queries use SQLAlchemy    │
   │  ORM, which automatically:              │
   │  - Escapes user input                   │
   │  - Prevents SQL injection               │
   │  - Uses parameterized queries           │
   └─────────────────────────────────────────┘


┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                    TECHNOLOGY STACK                         ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

FRONTEND:
  ├── React 18
  ├── Vite (build tool)
  ├── React Router (navigation)
  ├── Tailwind CSS (styling)
  └── Fetch API (HTTP requests)

BACKEND:
  ├── FastAPI (web framework)
  ├── Uvicorn (ASGI server)
  ├── SQLAlchemy (ORM)
  ├── Pydantic (validation)
  ├── Python-Jose (JWT)
  ├── Passlib (password hashing)
  └── Python 3.11+

DATABASE:
  ├── PostgreSQL 15
  └── Docker (containerization)

DEVELOPMENT:
  ├── Docker Desktop
  ├── Virtual Environment (venv)
  └── Hot Reload (both frontend & backend)


┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                    FILE STRUCTURE                           ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

PharmaPilot/
├── Client/                      (Frontend - React)
│   ├── src/
│   │   ├── components/
│   │   ├── context/
│   │   ├── pages/
│   │   ├── utils/
│   │   └── App.jsx
│   ├── package.json
│   └── vite.config.js
│
└── backend/                     (Backend - FastAPI)
    ├── app/
    │   ├── main.py             ⭐ FastAPI app
    │   ├── config.py           ⭐ Settings
    │   ├── database.py         ⭐ DB connection
    │   ├── models.py           ⭐ Database models
    │   ├── auth.py             ⭐ JWT auth
    │   └── routes/
    │       ├── auth.py         ⭐ Auth endpoints
    │       ├── projects.py     ⭐ Project endpoints
    │       └── agents.py       ⭐ Agent endpoints
    ├── requirements.txt        ⭐ Dependencies
    ├── docker-compose.yml      ⭐ Database config
    ├── .env                    ⭐ Environment vars
    ├── start.ps1               ⭐ Quick start script
    ├── test_backend.py         ⭐ Test suite
    └── README.md              ⭐ Documentation


┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                    DEPLOYMENT PORTS                         ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

┌─────────────────────┬──────────────┬──────────────────────┐
│ Service             │ Port         │ URL                  │
├─────────────────────┼──────────────┼──────────────────────┤
│ React Frontend      │ 5173         │ http://localhost:5173│
│ FastAPI Backend     │ 8000         │ http://localhost:8000│
│ API Documentation   │ 8000/docs    │ http://localhost:8000│
│                     │              │ /docs                │
│ PostgreSQL Database │ 5432         │ localhost:5432       │
│ Docker Desktop      │ Various      │ N/A                  │
└─────────────────────┴──────────────┴──────────────────────┘


┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                    SUCCESS METRICS                          ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

✅ Simple Architecture
   - Only 750 lines of backend code
   - 13 Python files (not 35+)
   - Easy to understand

✅ Production Ready
   - JWT authentication
   - Password hashing
   - CORS protection
   - Error handling
   - API documentation

✅ Frontend Integration
   - All endpoints match frontend needs
   - CORS configured
   - Response formats compatible

✅ AI Agent Ready
   - Simple execute interface
   - History tracking
   - Project-based organization

✅ Developer Experience
   - Quick start scripts
   - Comprehensive docs
   - Interactive API testing
   - Automated tests

✅ Scalable
   - Can add features later
   - Clean foundation
   - Not over-engineered


┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                    NEXT STEPS                               ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

1. Install Requirements:
   - Python 3.11+
   - Docker Desktop

2. Start Backend:
   cd backend
   .\start.ps1

3. Test API:
   http://localhost:8000/docs

4. Start Frontend:
   cd Client
   npm run dev

5. Test Integration:
   - Register account
   - Login
   - Use chat interface

6. Build AI Agents:
   - Call /api/v1/agents/execute
   - Store results
   - View history

🎉 You're ready to build!
```
